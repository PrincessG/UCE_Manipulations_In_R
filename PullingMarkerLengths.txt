#Commands that can be called within Terminal to isolate the length of each UCE marker in a given directory.
## Navigate to the directory that has each UCE nexus alignment file in it. 
grep nchar= *.nex > NameOfFile.psg

